set(music_install_dir ${KDE3_DATA_INSTALL_DIR}/boson/music)

# TODO: install all subdirectories that contain music files (*.ogg, maybe *.mp3)

message(STATUS "TODO: install music")

#install_files(${music_install_dir} ...)

