INCLUDES = -I$(top_srcdir)/bodebug $(all_includes)

bin_PROGRAMS = bouic

METASOURCES = AUTO

bouic_LDFLAGS = $(KDE_RPATH) $(all_libraries)
bouic_LDADD = \
		$(LIB_QT)
bouic_SOURCES = \
		bouic.cpp


