AC_DEFUN([AC_FIX_UIC],
[
  dnl this is an evil hack. we should not refer to the .libs dir directly. but
  dnl unfortunately there is no other way to access the .so file without
  dnl requiring to install it first :(
  boson_widgetdir="\$(top_builddir)/boson/bosonwidgets/plugin/.libs/"

  UIC="$UIC -L $boson_widgetdir"
])
AC_FIX_UIC


